export enum OperationType {
  Order, // 0 order on a variable order (debt)
  SubscriptionTotal, // 1 order on a CSA contract	(debt)
  Payment, // 2 payment of a debt
  Membership, // 3 membership (debt)
}
