export enum GroupFlags {
  __HasMembership = 0,
  ComputeMargin = 3,
  CamapNetwork = 4,
  CustomizedCategories = 5,
  HidePhone = 6,
  PhoneRequired = 7,
  AddressRequired = 8,
  UnUsed = 9,
  Show3rdCategoryLevel = 10,
}

export enum GroupBetaFlags {}

export enum RegOption {
  Closed = 0,
  WaitingList = 1,
  Open = 2,
  Full = 3,
}
